.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install torrentstream

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv torrentstream
    $ pip install torrentstream
